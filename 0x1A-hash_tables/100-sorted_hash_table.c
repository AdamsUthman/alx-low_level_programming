#include "hash_tables.h"

/**
 * shash_tables_create - creates a shash table with a given size
 *
 * @size: size of the shash table
 * Return: the creates shash tble, or NULL if function fails
 */
shash_table_t *shash_table_create(unsigned long int size)
{
	shash_tables_t *table;
	shash_node_t **array;
	unsignedlong int i;
}
