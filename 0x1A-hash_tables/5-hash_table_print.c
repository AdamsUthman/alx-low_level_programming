#include "hash_tables.h"

/**
 * hash_table_print - prints the keys and value of the hash tablle
 *
 * @ht: pointer to the hash table
 * Return: no return
 */
void hash_table_print(const hash_table_t *ht)
{
	unsigned long int i;
	hash_node_t *tmp;
	char *sep;
}
